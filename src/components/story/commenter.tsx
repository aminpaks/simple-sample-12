import * as React from 'react';
// import { FetchCommentQuery } from '../comment';
// import {
//   StyledStoryContainer as Container,
//   StyledStoryDetails as Details,
//   StyledStoryTitle as Title,
// } from './styled';

export const Comment = ({ id }: { id: string; loadComments?: boolean }) => (
  <div>TEST</div>
);
